# The Google Apps Script Web App URL (ends in /exec) that the signup form
# on the home page POSTs to (the script lives in the events@setsukohata.com
# Apps Script project). Leave unset locally and the form still works — it just won't
# record signups anywhere.
PUBLIC_SIGNUP_ENDPOINT=
